- plugin loading errors are not handled very well, need to test with modules that have syntax errors, raise other exceptions, have import errors, etc. Maybe figure out how to nicely say, could not load plugin, and here is the traceback.

- grouped by pay period summary output format and its requirements
    - Implementation wise, how should this data be grouped, what pay period function is needed?
    - Implement that function for each schedule
    - Pass the schedule object or just that function into the output?
    - Create a command pay period grouping function (probably)
    - make the output format
- documentation script for the output gallery, running example data. Probably not using a documentation library for this but writing my own custom format for this documentation. Maybe I can tap into the python docs system though.

Anytime in here too
- processors auto documentation lazydocs
